-- 테스트 에이전트 계정 생성 (auth.users + agents)
-- agent1@backoffice.local / agent123456
-- auth user는 run-sql.ps1로 직접 생성 불가 → API로 별도 생성 필요

-- 테스트 유저 계정 생성 (auth.users + user_profiles)
-- test@user.com / test123456
-- auth user는 API로 별도 생성 필요

-- 이 파일은 참조용. 실제 생성은 PowerShell 스크립트로 수행
SELECT 1;
